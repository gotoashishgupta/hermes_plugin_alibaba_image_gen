[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "requests>=2.31,<3",
    "pillow>=10",
    "pyyaml>=6",
    "python-dotenv>=1",
    "httpx>=0.27",
    "rich>=13",
    "prompt-toolkit>=3",
]

[tool.uv]
package = false
